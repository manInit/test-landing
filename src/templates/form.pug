mixin form()
  form.form
    label.form__input-group
      input(type="text", placeholder="Ваше имя*")
    label.form__input-group
      input(type="text", placeholder="Email*")
    label.form__input-group
      textarea(name="", rows="2", placeholder="Расскажите о своей компании*")
    button.form__submit.btn Подключиться
    
